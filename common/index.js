import { $confirm, $dialog, $error, $info, $success, $warning, appInit } from './tools';
import NotFoundVue from './components/404Error.vue';

import TableVue from './components/table-page/table-page.vue';

// 工具函数
export { $confirm, $dialog, $error, $info, $success, $warning, appInit };
// 组件
export { NotFoundVue, TableVue };
